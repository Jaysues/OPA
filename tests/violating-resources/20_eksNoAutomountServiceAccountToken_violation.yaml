# automount-sa-token-pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: automount-sa-token-pod
spec:
  containers:
    - name: nginx
      image: nginx
      # automountServiceAccountToken defaults to true
